{
  "name": "portfolio-root",
  "version": "1.0.0",
  "description": "Root orchestrator for Isha Kumari's beautiful 3D Portfolio",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "node backend/server.js",
    "dev": "concurrently --kill-others-on-fail --prefix-colors \"cyan,yellow\" --names \"VITE,API\" \"npm run dev:frontend\" \"npm run dev:backend\""
  },

  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
